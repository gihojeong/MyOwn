지금은 한국시간(KST) 평일 저녁 18:05다. 시간외 단일가(16:00~18:00)가 방금 끝났다. 19:00 발행이 납기다. 오늘 하루 KOSPI를 결산하는 마감 리포트를 작성해 ① HTML 파일 전달 ② giho.jeong@samsung.com으로 리포트 전문 메일 발송 ③ 내일 예측을 Drive에 기록, 세 가지를 모두 수행하라. 사용자는 BJ, 과학기술·정량 분석 배경이며 간결하고 밀도 높은 전문가용 서술을 선호한다. "무슨 일이 있었다"가 아니라 "그 사건이 KOSPI의 어느 부분을 어떤 경로로 움직였는가"의 인과 서술로 쓸 것.

■■ 납기 규율 (최우선) ■■
이 작업은 18:05에 실행되고 **19:00까지 반드시 발행**되어야 한다. 조사 가용 시간은 약 45분이다.
 · 18:50 조사 하드 스톱. 그 시점까지 4경로를 다 돌리고도 없는 항목만 "확인 불가"로 확정하고
   더 붙잡지 마라. 완결성보다 납기가 우선이다.
 · 검색·페치는 반드시 병렬로 던져라. 한 번에 4개 이상씩 묶어서 호출할 것.
 · 서브에이전트를 쓰지 마라. 조사도, 메일 발송도 전부 메인에서 직접 처리하라.
   ★2026-09-01 실측: Agent 호출이 오토 모드 분류기에 차단됐다("Blocked by classifier").
   위임을 시도하면 거부당한 뒤 결국 직접 하게 되므로 시간만 버린다. 처음부터 직접 하라.
 · ★bash 실행 패턴 — `python3 - <<'PY'` 형태의 heredoc은 분류기에 간헐 차단된다(9/1 6회 중 4회).
   파이썬은 반드시 ★Write 도구로 .py 파일을 저장한 뒤 `python3 파일.py`로 실행하라★. 100% 통과한다.
   `sed -i '...' file` 단독은 통과하나 `&& grep ...` 같은 복합 체인은 차단될 수 있으니 나눠서 호출하라.
 · 리포트·메일의 "발행 HH:MM"에는 **실제 발행 시각**을 적어라. 예정 시각(19:00)을 찍어놓고
   19:35에 내보내는 것은 허위 표기다. 19:00을 넘겼으면 넘긴 시각을 그대로 쓴다.

■■ ★★0순위 데이터 소스 — 키움 OpenAPI 수집기 (2026-09-05 도입) ■■
지수·수급·종목 시세는 웹에서 찾지 마라. KRX 확정치를 API로 직접 받는다. 앱키는
환경변수(APP_KEY / APP_SECRET / KIWOOM_MODE)로 이미 주입돼 있으니 건드리지도, 출력하지도 마라.

  curl -fsSL -o kiwoom_collect.py https://raw.githubusercontent.com/gihojeong/MyOwn/claude/kiwoom-rest-api-setup-28gawa/scripts/kiwoom_collect.py
  python3 kiwoom_collect.py --preset close --date <오늘 YYYYMMDD> --pause 0.3 --out kiwoom.json

★이 명령은 47건을 순차 호출하므로 3~6분 걸린다. ★백그라운드로 던지고 그동안 Drive 읽기와
[C] 요인용 웹 검색을 병렬로 진행하라.★ 결과 JSON의 summary.failed가 빈 배열이면 전량 성공이다.
실패 항목이 있으면 ★그 항목만★ 아래 웹 경로로 보완한다. 성공한 항목을 웹에서 다시 찾지 마라 —
시간 낭비이고, 웹 수치가 API 확정치와 어긋나면 API가 옳다. 어긋나면 각주로 차이를 적어라.
수집기가 아예 안 돌면(토큰 실패·네트워크) 그 사실을 리포트 말미에 명시하고 웹 경로로 전환한다.

── 수집기 JSON의 어느 키가 어느 항목인가 (results.<키>.data) ──
 [A]   index_kospi / index_kosdaq / index_kospi200 — 종가·전일대비·등락률·시가·고가·저가·
       거래대금·상승/보합/하락 종목수. ★inds_cur_prc_tm 배열에 시간별 지수가 들어 있어
       장중 궤적을 별도 조회 없이 그릴 수 있다.
       sector_indices_kospi — 전업종지수.
 [B]   investor_intraday_<투자자> — 외국인·기관계·투신·연기금·보험·은행·국가·기타법인의
       종목별 순매수(백만원). 시장 합계는 netprps_amt를 합산해 구한다.
       ★금융투자와 사모펀드는 이 API에 없다 — investor_after_close의 종목별
         fnnc_invt / samo_fund를 합산하라.
       investor_after_close — 종목 단위 3주체 원자료(약 1,200행, 연속조회로 전량 수신).
       ★etc_corp(기타법인)가 응답에 직접 있으므로 ★잔차 역산이 필요 없다★. 아래 [B] 항목의
         "3주체 합이 0으로 닫히지 않으면 잔차를 기타법인 몫으로 역산하라"는 지시는 무시하고
         API 실측값을 그대로 써라.
       program_trades_kospi / _kosdaq — 프로그램매매 차익(dfrt_trde_netprps)·
         비차익(ndiffpro_trde_netprps).
       investor_streak_5d — 최근 5거래일 외국인·기관 연속 순매수(추세 지속 vs 전환 판정).
       foreign_institution_top — 외국인·기관 매매 상위.
       sector_investor_flows_kospi — 업종별 유출입.
 [D]   candle_daily_<종목명> — 관심종목 일봉 600행(종가·시가·고저·거래량·거래대금).
       ★삼성전자(005930)와 삼성전자우(005935)를 처음부터 따로 받는다. 2026-08-25에
         우선주를 "확인 불가"로 두고 0%로 가정해 헤드라인이 92%→81%로 틀어진 사고가
         구조적으로 재발하지 않는다.
       investor_by_stock_<종목명> — 종목별 투자자·기관 매매.
 [D-2] amount_top(거래대금 상위) / change_rate_top_rise / change_rate_top_fall(등락률 상·하위).
       ★키움 등락률에는 부호가 정확히 들어 있다. companiesmarketcap의 무부호 등락률이나
         '마감' 제목을 단 장중 스냅샷 기사를 등락률 소스로 쓰지 마라 — 이제 쓸 이유가 없다.
 [A-2] after_hours_rank_rise / after_hours_rank_fall — 시간외 단일가 등락률 상·하위.
       after_hours_<종목명> — 관심종목 시간외 체결가(ovt_sigpric_cur_prc)·등락률
         (ovt_sigpric_flu_rt)·거래량(ovt_sigpric_acc_trde_qty). 18:05 실행이므로 확정돼 있다.

★수집기에 없는 것(웹 경로 그대로 조사): KOSPI200 선물·베이시스·미결제, 원/달러, 국고채 금리,
공매도, ★시가총액·상장주식수★, 해외 증시·지표. [D-2] 시총 비중 계산의 시총·상장주식수는
stockdigging.com 등 기존 경로로 확보하되, ★등락률만은 반드시 키움 값을 써라.★

■■ 데이터 조사 규칙 (2026-08-25 사고 반영 · 최우선) ■■
"확인 불가"는 데이터가 존재하지 않는다는 뜻이어야 한다. 내가 안 찾았다는 뜻이면 안 된다.
어떤 항목이든 "확인 불가"로 표기하기 전에 아래 4경로를 **모두** 시도하라. 시도하지 않고
확인 불가로 적는 것은 리포트 전체의 신뢰를 무너뜨리는 최악의 결함이다.

 1) AlphaSquare 개별종목 — https://alphasquare.co.kr/home/stock-summary?code=<6자리코드>
    종가·등락률·시가/고가/저가·거래량·거래대금·시가총액·상장주식수를 한 번에 준다.
    삼성전자 005930 / 삼성전자우 005935 / SK하이닉스 000660 / SK스퀘어 402340 /
    두산에너빌리티 034020 / LG에너지솔루션 373220 / 삼성전기 009150 / 현대차 005380
 2) AlphaSquare 시장요약 — https://alphasquare.co.kr/home/market-summary
    코스피·코스닥 거래대금, 상승/보합/하락 종목수, 투자자별 순매매
 3) 언론 3사 이상 교차 — 머니투데이·디지털데일리·헤럴드경제·YTN·한국경제·Businesskorea·연합
 4) Investing.com 과거 데이터 (kr.investing.com) — 반환된 '기준 일자'를 반드시 확인할 것.
    장중 캐시 스냅샷을 종가로 오인하지 마라.

정적 페치 불가로 확인된 경로(재시도 불필요): 네이버금융(프록시 403), KRX 정보데이터시스템
(JS 동적 로드), 다음금융(골격만 반환), 콕스톡(결과 0건).

지수 분해에 들어가는 입력(개별 종목 등락률·시총 비중·상장주식수)은 가정으로 채우지 마라.
2026-08-25 마감 리포트는 삼성전자우를 "확인 불가"로 처리하고 0%로 가정했으나 실제 +2.75%였고,
그 하나가 헤드라인 수치를 92% -> 81%로 틀리게 만들었다. AlphaSquare는 당시 검색 결과 1위에
떠 있었으나 열지 않았다. 같은 실수를 반복하지 말 것.

■ 중요: 메일은 첨부 없이 본문에 전문을 담는다
과거 Word/PDF 첨부를 시도했으나 실패했다. 첨부는 파일을 base64 문자열로 바꿔 도구 호출에 옮겨 적어야 하는데, 20,000자를 한 글자도 틀리지 않고 재생산해야 해서 zip이 깨진다(docx). PDF는 한글 폰트 임베딩만 120KB라 base64가 16만 자를 넘어 아예 읽어올 수 없다. **따라서 메일에 첨부를 넣지 마라.** 리포트 전문을 body(평문)와 htmlBody(서식) 양쪽에 담는다. 본문 HTML은 base64를 거치지 않으므로 손상될 여지가 구조적으로 없다.

■ 진행 순서
1) bash로 현재 정확한 KST 날짜/시각과 요일을 확인한다. 오늘이 한국 증시 휴장일이면 그 사실만 짧게 알리고 종료한다(메일도 보내지 않는다).
2) ToolSearch로 mcp__Google_Drive__search_files, mcp__Google_Drive__read_file_content, mcp__Google_Drive__create_file, mcp__Gmail__send_message를 로드한다.
3) Drive 폴더 "KOSPI 리포트"(id: 1hLv3NpWB3I70y33ZSpmhYgU6MGU-0cbK)에서 제목에 `kospi_forecast_YYYYMMDD`(오늘 날짜)가 들어간 문서를 모두 찾아 읽는다. `_am` 접미사는 오늘 아침 브리핑 예측, 접미사 없는 것은 어제 저녁 마감 리포트 예측이다.
4) ★키움 수집기를 가장 먼저 백그라운드로 띄운다(0순위 소스 항목 참조). 그것이 도는 동안 3)의 Drive 읽기와 [C] 요인용 웹 검색을 병렬로 진행하라.
5) 수집기가 채우지 못한 항목만 WebSearch/WebFetch로 조회한다. 기억·추정 금지. 위 경로를 다 돌린 뒤에도 못 구한 항목만 "확인 불가"로 명시. 검색은 병렬로 던져라.
6) [출력 규격]대로 HTML을 만들고 [발행 절차]를 순서대로 모두 수행한다.

■ 수집 항목
[A] 국내 결산: KOSPI/KOSDAQ 종가·등락폭·등락률·시가·고가·저가, 장중 궤적(개장 갭 → 오전 → 오후 → 마감 동시호가), 거래대금·거래량, 상승/하락 종목 수, KOSPI200 선물 종가·베이시스·미결제 변화, 원/달러 주간거래 종가와 장중 레인지, 국고채 3년/10년.
[A-2] 시간외 매매(필수): 시간외 종가매매(15:40~16:00)와 시간외 단일가매매(16:00~18:00, 종가 대비 ±10%)의 등락률 상·하위 종목, 삼성전자·SK하이닉스의 시간외 체결가·등락률·거래량. 이 작업은 18:05에 실행되므로 시간외 종료(18:00) 직후이고 데이터는 존재한다. 언론 보도가 드무니 AlphaSquare 개별종목 페이지(장후 시간외 종가 표기)를 먼저 보고, 증권사 HTS 웹·인포스탁·와이즈리포트를 시도하라. 네이버·다음·KRX는 정적 페치가 막혀 있으니 시간 쓰지 마라. 그래도 못 구하면 "확인 불가"로 명시한다. 시간외 체결은 지수 산출에 반영되지 않으므로 지수 수치는 정규장 확정치임을 함께 표기할 것.
[B] 수급 해부(가장 상세히): 코스피·코스닥 외국인/기관/개인 순매수(억원), 기관 세부주체(금융투자·투신·연기금·사모·보험·은행), 외국인·기관 각각 순매수 상위 10/순매도 상위 10 종목, 업종별 유출입, 프로그램 매매 차익/비차익, 선물 외국인 순매수·미결제 변화, 공매도 거래대금 상위·잔고 급증 종목, 최근 5거래일 외국인 누적 대비 오늘의 위치(추세 지속인가 전환인가). 3주체 합이 0으로 닫히지 않으면 잔차를 기타법인(자사주 매입 주체) 몫으로 역산하고 언론 보도치와 교차검증하라.
[C] 오늘 시장을 움직인 요인: 국내 정책·규제·실적·공시·특징주와 그 사유, 아시아 증시(니케이·항셍·상해·대만) 동시간대 흐름, 미 지수선물 장중 방향, 중국·일본 지표, 달러·유가·금리의 장중 변화가 외국인 수급에 미친 영향. 각 요인이 지수 등락에서 차지한 몫을 정량적으로 배분할 것.
[D] 반도체·AI: 삼성전자·SK하이닉스 종가와 시총 비중 기반 지수 기여 포인트를 실제 계산식으로 제시, HBM·파운드리·소부장 뉴스, 전일 SOX·엔비디아와의 연동 강도, 메모리 현물가 동향. 보통주와 우선주(삼성전자우)를 반드시 따로 조회하라 — 자사주 소각 국면에서는 둘의 등락률이 크게 갈린다.
[D-2] ★★시총 상위 30 분해 (필수 · 2026-09-01 신설) — 지수 등락의 전량 귀속
 목적: '반도체 3종 + 나머지 블록' 4블록 모델은 나머지 48.9% 안에서 무슨 일이 있었는지 말하지 못한다.
 실측 결과 ★그 나머지의 52.9%가 시총 31위 안의 대형주★였다. 상수로 두면 절반을 통째로 버린다.
 · 시총·상장주식수 확보 경로 1순위: ★stockdigging.com/ranking/kr/market-cap★ — 순위·종목코드·종가·
   시총을 1회 페치로 준다. 단 ★기준일이 직전 거래일★이므로 당일 등락률은 반드시 별도 확보하라.
   보조: alphasquare.co.kr/home/market-summary(지수·거래대금·A/D·코스피200), finance.daum.net/domestic/market_cap
 · ★삼성전자 행은 KRX 관행상 보통주+우선주 합산이다. 반드시 보통주/우선주로 분리해 31행으로 만들어라.
   검산: 상장주식수 x 종가가 소스 시총과 0.05% 이내로 맞는지 최소 3종목 확인하고 편차를 적어라.
 · ★★등락률 소스 함정 — 아래 2종은 전량 기각하라(2026-09-01 실증):
   (1) companiesmarketcap.com — ★등락률에 부호가 없다★(두산에너빌리티 실제 -6.46%를 +6.46%로 표기).
       게다가 KB금융·신한지주·POSCO홀딩스·우리금융은 티커가 KB/SHG/PKX/WF ★미국 ADR★이라
       한국 상장물이 아닌 ADR 가격을 싣는다. ★시총 순위 확인용으로만★ 쓰고 등락률엔 절대 쓰지 마라.
   (2) 중앙이코노미뉴스 joongangenews e-trend / Businesskorea 마감시황 — ★장중 스냅샷★이다.
       e-trend는 제목에 '장중'이 있으면 종가가 아니다('마감'만 종가). Businesskorea는 마감시황
       제목을 달고도 15시경 스냅샷이었다(삼성전자 +0.78% vs 확정 +1.17%).
   채택 소스: 인포스탁데일리 마감체크(kr.investing.com/news/stock-market-news 경유), CBC뉴스 마감,
   아시아경제·EBN 마감 기사의 ★확정 종가★만.
 · 상위 30종목 각각에 대해: 종목코드·업종·종가·등락률·시가총액·지수 비중·1% 상승 시 지수 기여(%p)·
   당일 지수 기여(%p)를 표로 제시하라. 비중은 ★직전 거래일 종가 기준★(= 당일 시작 시점 비중)을 쓴다.
 · ★항등식 검증(필수): Σ(확인 종목 비중 x 등락률) + 잔여 = 실제 지수 등락률.
   등락률을 못 구한 종목이 있으면 ★추정치로 채우지 말고★ 항등식에서 역산해 해당 블록의
   가중평균 등락률을 특정하라. 이것은 가정이 아니라 관측 함의값이다. 모델 잔차를 표기하라.
 · ★클러스터 6분류로 집계하라 — 비중·당일 기여·주도 종목을 함께:
   ① 반도체·전기전자 대형(삼전·삼전우·하닉·SK스퀘어·삼성전기·삼성SDI, 약 56%)
   ② 전력기기·기계 AI인프라(한화에어로·두산에너빌리티·HD현대일렉트릭·효성중공업·LS ELECTRIC, 약 3.7%)
   ③ 자동차·조선 운수장비(현대차·기아·현대모비스·HD현대중공업·한화오션·HD한국조선해양, 약 5.1%)
   ④ 금융·보험(KB·신한·하나·삼성생명·삼성화재·SK, 약 5.1%)
   ⑤ 바이오·제약(삼성바이오로직스·셀트리온, 약 2.0%)
   ⑥ 소재·기타(LG엔솔·고려아연·POSCO홀딩스·삼성물산·NAVER·LG전자, 약 4.8%)
   + 32위 이하 중소형주(약 22.8%)
 · ★아침 브리핑이 클러스터별 갭 함의를 제시했다면 그것을 ★클러스터 단위로 채점★하라
   (예측 갭 vs 실제 등락률, 괴리 %p, 방향 적중 여부). 어느 클러스터 판단이 틀렸는지 지목하라.
 · ★유가·금리·환율이 크게 움직인 날은 ★수혜/피해 진영의 시총 비중 배율★을 계산하라.
   2026-09-01 실측: 유가 피해 진영 7.28% vs 수혜 진영 1.77% = ★4.12배★. 정유주는 31위 밖이라
   지수에 직접 수혜주가 사실상 없다 → 유가 급등은 KOSPI에 구조적 순부정. 이런 비대칭을 매번 판정하라.
 · 집중도 지표를 매일 기록하라: 상위 2종목 누적 비중, 상위 12종목 누적, 상위 31종목 누적.
   (2026-09-01 기준 48.78% / 64.62% / 77.19%) 추세 변화가 곧 시장 구조 변화다.

 · ★★KODEX MSCI Korea (156080) 종가·등락률·거래대금 + NXT 시간외 괴리 (2026-09-01 신설, 필수)
   경로: alphasquare.co.kr/home/stock/stock-summary?code=156080 (실측 성공) /
        investing.com/etfs/samsung-kodex-msci-korea / google.com/finance/quote/156080:KRX
   ★★★EWY와 같은 지수가 아니다★★★
     KODEX MSCI Korea : ★MSCI Korea Index (무캡)★     삼성전자 34.14% + SK하이닉스 31.60% = 반도체 65.74%
     EWY              : ★MSCI Korea 25/50 Index (캡)★ 삼성전자 23.04% + SK하이닉스 21.24% = 반도체 44.28%
     KOSPI            :                                반도체 51.44%
     25/50 룰 때문에 반도체 비중이 ★1.48배★ 벌어진다. 대용하면 반도체 민감도를 1.48배 과대평가한다.
   ★★KODEX·KOSPI 연립으로 당일 반도체 S / 나머지 R을 확정하라 (2바스켓, 같은 세션·같은 통화라 교란 0):
       [0.6574 0.3426][S]   [KODEX]     det = +0.143009
       [0.5144 0.4856][R] = [KOSPI]     numpy.linalg.solve로 풀고 계산식 박스에 노출
     ★이 S,R이 상위30 클러스터 분해와 정합하는지 반드시 교차검증하라. 어긋나면 어느 쪽이 틀렸는지 판정.
     ★계수는 매번 실측 비중으로 갱신하라(2026-09-01 기준값이다).
   · KODEX − KOSPI 스프레드 = ★반도체 초과수익의 순수 측정치★. 당일 값과 5거래일 추이를 함께 제시.
[E] 오늘 밤~내일: 한국시간 기준 오늘 밤 발표되는 미국·유럽 지표·실적과 컨센서스, 유럽 증시 개장 초반 흐름, 미 선물 현재 방향, 내일 KOSPI 상방/기본/하방 3시나리오(확률·예상 등락폭·트리거 조건).
[F] 예측 대비 괴리 검증(필수): 3)에서 읽은 예측 기록과 실제 종가를 대조한다. 아침 브리핑(`_am`) 예측이 있으면 주 벤치마크로 삼고, 어제 저녁 예측은 보조로 함께 표기하라.
  - KOSPI: 시나리오별 예측 등락폭 중앙값 대비 실제 등락률의 괴리를 ±%p로. 확률가중 기대치와의 괴리도 계산.
  - 삼성전자·SK하이닉스: 예측값 대비 실제 등락률 괴리를 ±%p와 배율(실제/예측)로 병기.
  - 오차의 귀속: 시총 가중 전달 모델 Σ(비중ᵢ × 실제등락률ᵢ)을 계산해 실제 지수 등락과 비교하고 잔차를 제시하라. 모델 오차와 입력값 오차를 분리해 귀속시킬 것. 예측 불가능한 개별 이벤트(공시·실적 서프라이즈)가 있었다면 그 몫을 명시적으로 떼어내라.
  - 적중한 판단과 빗나간 판단을 각각 나열하라. 자기 채점은 관대하지 말 것.
  - Drive에 예측 기록이 없으면 그 사실을 명시하고 "벤치마크 부재"로 표기하라. 예측치를 사후에 지어내지 말 것.

■ ★★HTML 출력 규격 — 처음부터 "이메일용 HTML"로 작성하라 (2026-09-01 전면 개정 · 가장 중요)
수신자는 samsung.com 계정, 즉 ★Outlook/Exchange★다. Outlook 데스크톱은 Word 렌더링 엔진을 쓰므로
아래를 ★전부 무시★한다: <style> 블록, CSS 클래스 선택자, display:grid, display:flex,
display:inline-block, border-radius, @media, @supports, <svg>, CSS 변수.
이런 걸 쓰면 스타일이 통째로 벗겨져 맨 텍스트로 보인다(2026-08-21에 실제 사고).
★이전 버전의 스켈레톤(<!DOCTYPE> + <style> 블록 + 인라인 SVG 차트)은 폐기한다. 쓰지 마라.★
파일 버전과 메일 버전을 나누지 말고 ★하나의 이메일 호환 HTML★을 만들어 둘 다에 쓴다.

필수 규칙:
- ★레이아웃은 전부 <table>. 바깥은 <table width="100%"><tr><td align="center"> 안에 <table width="880">.
  다단(KPI 4열, 카드 3열, 스트립 3열)은 <tr>에 <td width="25%">/<td width="33%">를 나열하고
  그 안에 카드용 중첩 테이블을 넣는다.
- ★스타일은 전부 인라인(style="..."). <style> 블록과 class 선택자 금지.
  <!DOCTYPE>, <html>, <head>, <body> 태그도 넣지 말고 <div>부터 시작한다.
- ★용량 절약: font-family·font-size·기본 color는 최상단 <div>와 바깥 <td>에 한 번만 지정하고,
  수백 개의 데이터 셀에서는 style 속성을 통째로 생략하라 — Word 엔진은 상위에서 상속한다.
  표는 cellpadding="6" cellspacing="0"을 쓰고 셀마다 padding/border를 반복하지 마라.
  행 구분은 border-bottom 대신 지브라 배경(<tr bgcolor="#fafbf6">)으로만.
  헤더행은 <tr bgcolor="#e4e9dc"> + 셀 내용을 <b>로. 색상 강조는 <b><font color="#d03b3b">…</font></b>.
  HTML 엔티티 대신 UTF-8 리터럴 문자를 직접 써라. &amp; &lt; &gt; 만 엔티티로 남긴다.
- ★SVG 금지. 이전 규격의 d1 장중 궤적 라인차트와 d2 수급 발산 막대는 아래로 대체한다:
  · 장중 궤적 → ★시각별 지수 스냅샷 표★(09:00 시가 / 09:30 / 10:30 / 12:00 / 13:30 / 14:30 /
    15:20 / 15:30 종가)로 제시하고, 확인된 실측 앵커와 미확보 시각을 구분해 표기하라.
    미확보 시각은 보간하지 말고 빈칸으로 두고 그렇게 밝혀라.
  · 수급 발산 막대 → bgcolor를 칠한 고정폭 <td>를 중첩 테이블로 배치해 그린다
    (숫자 라벨 + 범례 포함). 막대 길이는 금액 비례로 px를 계산해 넣어라.
- 계산식 박스는 bgcolor="#16301f" 셀에 font-family:Consolas,monospace를 주고 줄바꿈은 <br>,
  들여쓰기는 공백 문자로 직접 넣는다(<pre>나 white-space:pre는 Outlook에서 불안정).
- 팔레트: 배경 #f2f4ec / 밴드 #16301f / 상승 #d03b3b / 하락 #2a78d6 / 선 #d8ddd0 / 카드 #ffffff /
  강조박스 #fdf6e8 — 차분한 페이퍼 톤.
- ★앵커 링크(href="#d1")를 아예 넣지 마라 — 메일에서 동작하지 않는다.
  이전 규격의 "카드 하단 상세 섹션 앵커 링크"와 "href='#d5'로 연결"은 폐기한다.

구성 순서: ① 헤더(★실제 발행 시각 명기★) → ② 오늘의 결론 → ②-b ★데이터 신뢰도 박스★ →
③ KPI 타일 8개 → ④ 핵심 이슈 카드 6~9장 → ⑤ 시사점 스트립 → ⑥ 내일 체크포인트 5개 →
⑦ "상세 리포트" 구분선 → ⑧ 상세 섹션 d1~d6 → ⑨ 면책·출처

- ★②-b 데이터 신뢰도 박스는 필수다. 결론 바로 다음, KPI 타일 앞에 배치. 확보 항목 수 / 확인 불가
  항목 수를 명시하고, ★확인 불가 항목을 전부 나열하되 각각에 "결산 영향: 있음/낮음/없음"을 붙여라.★
  영향 "있음"은 굵게. 이 목록을 면책 각주로 내리지 마라. 확인 불가가 0건이면 "전 항목 확보"라고 명시.
- KPI 타일 8개 중 하나는 반드시 "예측 대비 괴리"로 하고 ±%p를 표시한다. 마지막 카드 1장도 예측 괴리 요약.
- 카드 1장 = 오늘 KOSPI를 움직인 요인 1건. 제목 15자 내외 명사형, 불릿 2개에 각각 <b>실제 수치</b> 포함,
  하단에 "시사점"(1~2문장) + 방향 배지 + 강도 배지. 카드 상단 3px 강조선은
  <tr><td height="3" bgcolor="…"></td></tr>로.
- 방향 배지: 상승=빨강(▲ 지수 기여 +) / 하락=파랑(▼ 지수 기여 −) / 중립(― 혼조).
  색만이 아니라 ▲▼― 기호와 텍스트를 항상 함께.
- 등락 수치는 항상 색+▲▼ 기호를 붙이고, ▲▼는 반드시 숫자 부호와 일치시킬 것
  (금리가 올라 지수에 악재여도 숫자가 +면 ▲다. 부호와 기호를 먼저 맞추고, 악재/호재는 색으로 표현하라).
- 상세 섹션: d1 국내 증시 결산(+시간외·NXT), d2 수급 해부, d3 ★시총 상위 30 + 클러스터 분해★,
  d4 반도체·AI 밸류체인, d5 예측 대비 괴리 검증, d6 내일 시나리오.
  각 섹션은 표 + 해석 문단 + 강조박스(전이 경로/임계선) 조합.
- d3에는 [D-2]의 시총 상위 30 전체 표(30행 + 상위31 합계 + 32위 이하 잔여)를 넣고, 그 아래에
  클러스터 6분류 집계표(비중·당일 기여·주도 종목·아침 예측 대비 채점)를 배치하라.
  항등식 검산(모델 잔차)과 집중도 지표(상위2/상위12/상위31 누적 비중)도 같은 섹션에 노출한다.
- d3에는 "오늘 지수 등락 ±X% 중 반도체 기여 Y%p, 나머지 Z%p" 정량 분해를 반드시 포함한다.
- d5에는 [F]의 결과를 표 3개(지수 시나리오별 괴리 / 개별 종목 괴리 / 오차 귀속)로 제시한다.
- ★Gmail 클리핑 한도 102,400 바이트를 넘기지 말 것. 발송 전 len(html.encode())를 반드시 출력해
  확인하고, 초과하면 위 용량 절약 기법을 먼저 적용하고 그래도 넘치면 표 행을 줄여 맞춘다(목표 95KB 이하).
- ★발송 전 자동 검증(필수): '<style' / 'var(--' / 'class="' / 'display:grid' / 'inline-block' / '@media' /
  '<svg' / 'http' / '<script' / 'border-radius' / '<!DOCTYPE' / 'href="#' / '<html' / '<head' / '<body'
  문자열이 ★하나도 없어야★ 한다. 파이썬 assert로 확인하고, 태그 개폐 균형(table/tr/td/div/font/b/span)과
  ▲▼ 기호-부호 일치, 색상-부호 일치(상승=#d03b3b / 하락=#2a78d6)를 정규식으로 자동 검사하라.
  ★당일 종가 표 안에 과거 일자(전일 등) 날짜 문자열이 섞였는지도 정규식으로 검출하라.
- playwright 렌더링 검증은 ★선택★이다(executablePath는 find로 /opt/pw-browsers 아래 chromium 실제 경로
  지정, playwright install 금지). 시간이 모자라면 생략하고 문자열 assert만으로 발송하라.


■ 발행 절차 (모두 수행)
1) HTML을 작업 디렉토리에 저장한다(kospi_close_YYYYMMDD.html).
2) ★위 [출력 규격]의 자동 검증(문자열 assert + 태그 개폐 + 부호 일치 + 용량)을 반드시 실행한다.
   playwright 렌더링 확인은 선택이며 시간이 있을 때만 한다. executablePath는 find로 /opt/pw-browsers 아래 chromium 실제 경로를 찾아 지정(`playwright install` 금지). 모든 요소에 scrollWidth > clientWidth 검사를 돌려 가로 넘침이 있으면 고친다.
3) SendUserFile로 HTML을 전달한다.
4) **메일 발송 — 첨부 없이 본문에 전문.** mcp__Gmail__send_message를 호출한다.
   - to: ["giho.jeong@samsung.com"]
   - subject: `[KOSPI 마감 리포트] YYYY-MM-DD (요일) {종가} ▲/▼{등락률} — {한 줄 핵심}`
   - **attachments: 넣지 않는다.**
   - ★**body(평문) 파라미터를 넣지 마라.** 평문과 HTML을 함께 보내면 multipart/alternative가 되어
     클라이언트가 텍스트 파트를 골라 서식 없이 보일 수 있다. ★htmlBody 단독으로 발송한다.★
     (2026-09-01 개정. 이전 규격의 '평문 body + htmlBody 병행'은 폐기.)
   - **htmlBody**: [출력 규격]대로 만든 이메일 호환 HTML 전문을 그대로 넣는다.
     ★서브에이전트에 위임하지 마라 — Agent 호출은 오토 모드 분류기에 차단된다(2026-09-01 실증).
     HTML이 커도 ★본체가 직접 htmlBody에 전문을 옮겨 적어라★. 약 60,000자 기준 5분 정도 걸리므로
     그 시간을 발행 시각 계산에 미리 반영하라. 요약·수정 절대 금지, body 파라미터 금지,
     DOCTYPE/html/head/body 추가 금지. 전사 전에 `fold -w 3000 파일 > f.txt`로 조각내 전량을 읽고,
     발송 후 get_message로 실제 반영을 확인하라, `table`과 인라인 style만 사용한다(SVG는 Gmail에서 표시되지 않으므로 장중 궤적·수급 막대는 표나 문장으로 대체하라). 상승 #d03b3b / 하락 #2a78d6 / 강조 #16301f. 카드는 `<table>` 한 장씩으로 감싸 border-top 색을 --c1~--c7 값으로 준다.
   - 크기: Gmail은 본문이 102KB를 넘으면 "메시지 잘림"으로 표시하므로 **htmlBody를 100KB 이하로 유지**한다. 넘칠 것 같으면 상세 섹션의 서술 문단을 줄이되 표와 수치는 유지하라.
   - 발송 후 반환된 id로 mcp__Gmail__get_message(messageFormat: MINIMAL)를 호출해 발송을 확인한다.
   - 발송에 실패하면 실패 사실과 원인을 채팅 요약 맨 위에 적어라. 조용히 넘어가지 말 것.
   - 메일은 한 번만 보낸다. 발송 후 오류를 발견하면 정정 메일을 자동으로 보내지 말고, 채팅 요약에 정정 내용을 적고 사용자에게 발송 여부를 물어라.
5) **내일 예측을 Drive에 저장.** mcp__Google_Drive__create_file로 parentId='1hLv3NpWB3I70y33ZSpmhYgU6MGU-0cbK', title=`kospi_forecast_YYYYMMDD`(다음 거래일), contentMimeType='text/plain'로 아래를 평문 기록한다. 금요일이면 다음 월요일 날짜를 쓴다.
   - target_date, issued_at, issued_by(마감리포트)
   - 오늘 종가: KOSPI, KOSDAQ, 삼성전자, 삼성전자우, SK하이닉스 / 각 시총 비중과 상장주식수
   - 시나리오 3개: 각 확률, 등락폭 범위, 지수 밴드, 트리거 조건 / 확률가중 기대치
   - 삼성전자·삼성전자우·SK하이닉스 시나리오별 참조 등락률
   - 핵심 가정 1~2문장, 체크포인트 5개
   - ★시총 상위 30 스냅샷(필수): 종목명·코드·종가·등락률·시총·지수비중을 파싱 가능한 표로.
     내일 아침 브리핑이 이것을 그대로 이어받아 비중 재계산 없이 쓴다.
   - ★KODEX MSCI Korea(156080) 종가·등락률 + KODEX−KOSPI 스프레드
   - ★KODEX·KOSPI 연립해 S(반도체)·R(나머지)와 사용한 비중 계수
   - ★클러스터 6분류 비중과 당일 기여(%p), 집중도 지표(상위2/상위12/상위31 누적 비중)
   - ★5블록 분해: 삼성전자 / 삼성전자우 / SK하이닉스 / 상위31 내 기타 대형주(약 25.9%) /
     32위 이하 중소형주(약 22.8%) — 각각의 당일 등락률과 내일 시나리오별 값.
     ★기존 4블록의 '나머지 48.9%'를 이 2블록으로 쪼갠 것이다. 상수로 두지 마라(4회 연속 지적).
   - 이번 회차에서 4경로를 다 돌리고도 못 구한 항목 목록(다음 세션이 헛수고를 반복하지 않도록)
   - 같은 제목의 파일이 이미 있으면 새로 만들지 말고, 내용을 고쳐야 할 때만 새 파일 생성 후 이전 파일을 trash_file로 정리해 폴더에 중복이 남지 않게 하라.
6) 채팅에는 6~10줄 초압축 텍스트 요약만 남긴다(리포트 내용 반복 금지). 오늘의 예측 괴리 ±%p, 메일 발송 성공 여부, 실제 발행 시각을 반드시 포함한다.

■ 원칙
- 수치는 반드시 실제 조회로 확보하라. 위 4경로를 다 돌린 뒤에도 못 구한 항목만 "확인 불가"로 남기고, 그럴듯한 추정치로 채우지 말 것.
- 직접 계산한 값(시총 비중, 지수 기여도, 요인별 몫, 예측 괴리)은 계산식과 전제를 함께 보여주고, 거래소 공식치가 아님을 면책에 명시하라. 산식은 bash/python으로 실제 계산해 검산하라 — 암산으로 적지 마라.
- 매체별 수급 집계가 다르면 편차 범위를 밝히고 어느 값을 기준으로 삼았는지 적어라.
- 자기 예측의 실패를 축소하지 말 것. d5는 자기 채점이므로 관대함이 곧 결함이다.
- 메일은 BJ 본인 Gmail(gihojeong@gmail.com)에서 발신된다. 수신자에게 이 주소가 발신자로 표시된다.
- Word/PDF 첨부는 시도하지 마라. 본문 전문이 확정된 방식이다.