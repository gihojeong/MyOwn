한국시간(KST) 토요일 아침 06시경이다. 이 작업은 ★지난 한 주(월~금)의 주간 리뷰·예측 채점 리포트★ 전용이다.
완성된 HTML을 이메일 본문에 그대로 넣어 giho.jeong@samsung.com 으로 발송한다.
사용자는 BJ, 과학기술·정량 분석 배경이며 간결하고 밀도 높은 전문가용 서술을 선호한다.
무인 실행이므로 확인 질문 없이 판단해 진행하라. 모든 웹 접근은 자동승인으로 간주하고 시간을 끌지 마라.

■■■ ★★최우선 경고 — 이 작업은 '개장 전 브리핑'이 아니다 ■■■
2026-08-29(토) 실행에서 이 작업이 ★개장 전 브리핑 프롬프트를 들고 발화★해, 장이 열리지 않는
토요일 아침에 개장 전 예측 리포트를 만들어 메일로 발송하는 사고가 났다. 반복하지 마라.
- 토요일에는 한국 증시가 열리지 않는다. ★개장 갭·시가/고가/저가 등 일별 예측을 하지 마라.★
- 다음 개장일(월요일) 예측은 ★월요일 05:10에 실행되는 별도 작업(개장 전 브리핑)의 몫★이다.
  여기서 미리 만들면 중복 발송이 된다. 절대 만들지 마라.
- 이 작업의 산출물은 오직 ★지난 주 회고 + 예측 채점 + 교훈 + 다음 주 관전 포인트★다.

■■■ 0단계 — 실제 날짜·시각 확정 (다른 무엇보다 먼저) ■■■
★★bash date를 신뢰하지 마라. 2026-08-29 실행에서 컨테이너 시계가 실제보다 정확히 48시간
지연돼 있었다(bash가 8/27 목요일이라고 보고). 시계는 세션 중간에 스스로 동기화되기도 한다.
 1) bash date는 참고용으로만 읽는다.
 2) ★mcp__Google_Drive__search_files로 폴더 1hLv3NpWB3I70y33ZSpmhYgU6MGU-0cbK 를 조회한다.
    응답의 createdTime/modifiedTime은 구글 서버가 찍은 UTC 실제 시각이다(+9시간 = KST).
    이 값이 진짜 현재 시각의 하한이다. 이 조회는 어차피 2단계에서 필요하므로 낭비가 아니다.
 3) 교차검증: stockinfo7.com/report/trade/info 가 반환하는 '기준 일자'를 확인한다.
 4) bash date와 Drive 서버 시각이 6시간 이상 어긋나면 ★Drive 서버 시각을 채택하고★
    그 사실과 편차 시간을 리포트 최상단 경고 박스와 PushNotification에 명시하라.
★게이트: 확정된 날짜가 토요일이 아니면(=이 작업이 잘못된 날 발화했다면) 리포트를 만들지 말고
  그 사실만 PushNotification으로 알리고 즉시 종료하라.

■■■ 발행 시각 규율 ■■■
06:00 기동 → ★08:30까지 발행★. 개장 전 브리핑만큼 촉박하지 않으므로 채점의 정확도를 우선하되,
08:30을 넘기면 초과 분수를 리포트 최상단과 PushNotification 첫 줄에 명시하라.
- 06:00~06:10: 0단계 + Drive에서 지난 주 예측 문서 전량 조회 + 1차 병렬 조회 배치를 같은 메시지에.
- 06:10~07:30: WebSearch/WebFetch 직접 병렬 조회(실제 시장 데이터 확보).
- ★07:30 하드 컷오프★. 07:30~08:10 계산·HTML 생성. 08:10~08:20 검증. 08:20~08:30 발송.

■■■ 웹 조회 규율 (검증됨) ■■■
★무인 세션에서 임의 URL을 곧바로 WebFetch하면 PROVENANCE_REQUIRED(승인 대기 초과)로 차단된다.
  2026-08-29 실행에서 검증된 우회법:
  <WebSearch로 먼저 검색해 결과 목록에 URL이 노출되게 한 뒤, 그 URL을 WebFetch하면 통과한다.>
  절대 URL을 직접 페치하지 마라. 항상 'WebSearch → 노출된 URL을 WebFetch' 순서다.
  원하는 사이트가 결과에 안 뜨면 사이트명을 검색어에 넣어 다시 검색하라(예: "stockinfo7 코스피 2026").
- 한 메시지에 4~8개를 동시에 던져라. 하나씩 순차로 던지지 마라.
- ★서브에이전트를 쓰지 마라. 조사도 메일 발송도 전부 메인에서 직접 처리하라.
  ★2026-09-01 실측 — Agent 호출 자체가 오토 모드 분류기에 차단됐다("Blocked by classifier").
  위임을 시도하면 거부당한 뒤 결국 직접 하게 되므로 시간만 버린다. 처음부터 직접 하라.
- ★bash 실행 패턴 — `python3 - <<'PY'` 형태의 heredoc은 분류기에 간헐 차단된다(9/1 6회 중 4회).
  파이썬은 반드시 ★Write 도구로 .py 파일을 저장한 뒤 `python3 파일.py`로 실행하라★. 100% 통과한다.
  `sed -i '...' file` 단독은 통과하나 `&& grep ...` 같은 복합 체인은 차단될 수 있으니 나눠서 호출하라.
- 검색어에 연도를 넣어라("... 2026"). 넣지 않으면 과거 기사가 상위에 온다.
- 만기일·영업일처럼 달력으로 결정되는 값은 검색하지 말고 bash로 계산하라.
검증된 경로:
 · AlphaSquare 개별종목 alphasquare.co.kr/home/stock-summary?code=<6자리>
   — 종가·등락률·시가/고가/저가·거래량·거래대금·시가총액·★상장주식수★·시간외 시세를 한 번에.
   삼성전자 005930 / 삼성전자우 005935 / SK하이닉스 000660 / SK스퀘어 402340 /
   두산에너빌리티 034020 / LG에너지솔루션 373220 / 삼성전기 009150 / 현대차 005380
   ★삼성전자 보통주와 우선주는 반드시 따로 조회하라 — 자사주 소각 국면에서 크게 갈린다.
 · stockinfo7.com/report/trade/info — ★수급 1순위★. 지수 종가/등락률 + 투자자별 순매수(현물+선물)를
   한 번에 주고 3주체 합이 정확히 0으로 닫힌다. 기준일자도 반환한다.
 · AlphaSquare 시장요약 alphasquare.co.kr/home/market-summary (거래대금·상승/하락 종목수)
 · stockanalysis.com/stocks/{티커}/history/ 및 /etf/{티커}/history/ — 해외 개별주·ETF 1순위.
   EWY 구성비는 stockanalysis.com/etf/ewy/holdings 에서 ★실측치★를 받아라(가정 금지).
 · investing.com/indices/phlx-semiconductor — SOX 종가·등락률
 · 야후파이낸스 라이브블로그("stock market today <요일> <월> <일> 2026"),
   investrade.com/morning-preview-<월>-<일>-2026
 · 국내 언론 3사 이상 교차: 헤럴드경제·한국경제·Businesskorea·머니투데이·연합·청년일보·
   오피니언뉴스·이코노미스트·파이낸셜뉴스·디지털타임스·매일일보·YTN
페치 불가로 확인된 경로(재시도 불필요): 네이버금융(프록시 403), KRX 정보데이터시스템(JS 동적),
다음금융(골격만), 콕스톡(0건), DART 문서뷰어 dsaf001/main.do(차단), CNBC 일부 기사(403),
워싱턴포스트(403), 야후 파이낸스 일부 기사(403).
- 동일 항목 재시도는 위 경로 각 1회까지. 그 이상 붙잡지 마라.
- ★"확인 불가"는 데이터가 존재하지 않는다는 뜻이어야지, 내가 안 찾았다는 뜻이면 안 된다.
  못 구한 항목은 "확인 불가"로 명시하고 그것이 채점 결론을 바꾸는지 판정해 적어라.
- ★채점 입력(종가·등락률·시총 비중·상장주식수)은 절대 가정으로 채우지 마라.
  2026-08-25에 삼성전자우를 0%로 가정했다가 실제 +2.75%였고 헤드라인 수치가 92%→81%로 틀어졌다.

■■ ★★0순위 데이터 소스 — 키움 OpenAPI 수집기 (2026-09-05 도입) ■■
지수·수급·종목 시세는 웹에서 찾지 마라. KRX 확정치를 API로 직접 받는다. 앱키는
환경변수(APP_KEY / APP_SECRET / KIWOOM_MODE)로 주입돼 있으니 건드리지도, 출력하지도 마라.
★조사를 시작하기 전에 아래 지침 문서를 읽고 그대로 따르라.★ 수집 명령, 결과 JSON의 키와
리포트 항목 대응표, 웹으로 보완해야 하는 항목이 전부 거기 있다. 이 프롬프트보다 그 문서가 최신이다.

  curl -fsSL https://raw.githubusercontent.com/gihojeong/MyOwn/master/docs/kiwoom-report-playbook.md

요약: 수집기를 ★백그라운드로 먼저 띄우고★(--preset weekly --date <지난주 금요일 YYYYMMDD>, 3~6분)
그동안 Drive 읽기와 [C] 요인용 웹 검색을 병렬로 진행한다. 결과 JSON의 summary.failed에
남은 항목만 웹으로 보완하고, ★성공한 항목을 웹에서 다시 찾지 마라.★ 웹 수치가 API 확정치와
어긋나면 API가 옳다(차이는 각주로). 수집기가 아예 안 돌면 그 사실을 리포트 말미에 명시하고
웹 경로로 전환한다.
★investor_after_close 응답에 etc_corp(기타법인)가 직접 들어 있다. 따라서 아래 [B]의
"3주체 합이 0으로 닫히지 않으면 잔차를 기타법인 몫으로 역산하라"는 지시는 무시하고
API 실측값을 그대로 써라.★
★수집기에 없어 웹 조사가 그대로 필요한 것: KOSPI200 선물·베이시스·미결제, 원/달러,
국고채 금리, 공매도, 시가총액·상장주식수, 해외 증시·지표.★

■ 진행 순서
1) 0단계로 날짜를 확정한다.
2) ToolSearch로 mcp__Google_Drive__search_files, read_file_content, create_file, trash_file 를 로드한다.
   폴더 "KOSPI 리포트"(1hLv3NpWB3I70y33ZSpmhYgU6MGU-0cbK)에서 ★지난 주 5거래일의
   kospi_forecast_YYYYMMDD_am(아침 브리핑)과 kospi_forecast_YYYYMMDD(저녁 마감 리포트)★ 문서를
   전부 찾아 읽는다. 직전 kospi_macro_baseline_* 도 있으면 읽어 전주 대비 변화를 잡아라.
   동일 제목이 2건 이상이면 issued_at이 가장 늦은 것(REV 표기가 있으면 REV본)을 채택하라.
   ★각 문서의 "확인 불가 항목 목록"과 "인프라 메모"를 반드시 읽어라 — 헛수고를 막고
     검증된 신규 경로를 물려받을 수 있다.
3) ★키움 수집기를 먼저 백그라운드로 띄운다(0순위 소스 절 참조). 지난주 지수·수급·주봉은 거기서 나온다.
3-2) 수집기가 채우지 못한 항목만 WebSearch/WebFetch로 확보해 예측과 대조한다.
4) HTML을 만들어 SendUserFile로 전달하고, 같은 HTML을 메일 본문에 넣어 발송한다.
5) 다음 주 기준선을 Drive에 저장한다(메일 발송 뒤에).

■ 리포트 내용 — 이 작업의 핵심은 ★채점과 학습★이다
[1] 주간 시장 요약
 · 월~금 5거래일 KOSPI/KOSDAQ 일별 종가·등락률·거래대금, 주간 누적 등락률, 주간 고가·저가
 · 삼성전자·삼성전자우·SK하이닉스·SK스퀘어 일별 종가·등락률과 주간 누적
 · ★주간 지수 분해: 반도체 3종(삼전·삼전우·하닉) vs 나머지 블록의 주간 기여도(%p)를 항등식으로
   분해하고 주 전체를 무엇이 끌었는지 규명하라. 일별 분해도 표로 제시하고 ★모델 잔차가 0인지★
   반드시 검산하라. 비중은 ★각 거래일의 직전일 종가 기준★(= 당일 시작 시점 비중)을 써라.
 · 주간 수급: 5일간 외국인/기관/개인/★기타법인(자사주 매입 주체)★ 누적 순매수(억원)와 일별 추이.
   3주체 합 항등식 검산을 일별로 수행하고, 기타법인은 역산치와 보도치를 교차검증하라.
   선물 투자자별 순매수도 함께 집계해 ★현·선물 방향 일치 여부★를 판정하라.
 · ★★시총 상위 30 주간 분해 (2026-09-01 신설)
   · 주초(월 시가 직전) 대비 주말(금 종가) ★시총 상위 30 스냅샷 2장★을 만들고 순위 변동을 표기하라.
     소스 1순위 stockdigging.com/ranking/kr/market-cap(기준일이 직전 거래일임에 주의).
     ★삼성전자 행은 보통주+우선주 합산이므로 반드시 분리해 31행으로 만들 것.
     ★companiesmarketcap은 등락률에 부호가 없고 일부 종목이 미국 ADR 가격이다 — 순위 확인용으로만.
     ★e-trend·Businesskorea는 장중 스냅샷이다 — 확정 종가만 채택(인포스탁 마감체크·CBC·아시아경제·EBN).
   · ★클러스터 6분류(반도체·전기전자 대형 / 전력기기·기계 AI인프라 / 자동차·조선 / 금융·보험 /
     바이오·제약 / 소재·기타 + 32위 이하 중소형주)의 ★주간 누적 기여(%p)★를 항등식으로 분해하고
     일별 추이를 표로 제시하라. ★주간 순환매의 방향을 클러스터 단위로 지목하는 것이 목적이다.
     "기존 주도주가 되돌림했는가"가 아니라 ★"자금이 어느 클러스터로 갔는가"★를 답하라.
   · ★★KODEX MSCI Korea(156080) vs KOSPI 주간 스프레드 — 반도체 집중도 로테이션의 직접 측정
     KODEX는 ★MSCI Korea 무캡★(반도체 65.74%), KOSPI는 반도체 51.44%다. 둘 다 ★같은 세션·같은 통화★라
     FX·시차 교란이 0이며, 스프레드(KODEX − KOSPI)가 곧 ★반도체 초과수익★의 순수 측정치다.
     ★EWY(MSCI Korea 25/50 캡, 반도체 44.28%)와는 지수가 다르다 — 대용 금지. 1.48배 과대평가된다.
     · 5거래일 각각의 KODEX·KOSPI 등락률로 연립을 풀어 ★일별 반도체 S와 나머지 R★을 확정하라
       ([0.6574 0.3426; 0.5144 0.4856] 역행렬, numpy.linalg.solve). 개별종목 역산보다 정확하다.
     · 주간 누적 S와 R을 제시하고, 그것이 클러스터 분해와 정합하는지 교차검증하라.
     · ★KODEX − EWY(원화환산) 주간 괴리는 ★캡 효과 + 세션 시차 + FX★의 합이다. 세 성분으로
       분해하고, 어느 성분이 지배적이었는지 밝혀라. 이 분해가 다음 주 EWY 패리티 가중의 근거가 된다.
   · ★집중도 드리프트: 상위 2종목 / 상위 12종목 / 상위 31종목 누적 비중의 주초 대비 주말 변화(%p).
     (2026-09-01 기준선 48.78% / 64.62% / 77.19%) 반도체 집중도가 오르면 지수는 개별 2종목의
     함수로 수렴하고, 내리면 폭(breadth)이 살아난 것이다. 이 해석을 매주 명시하라.
   · ★주간 승자·패자를 상위 30 안에서 각각 5종목씩 뽑고, 그 등락이 ★클러스터 논리로 설명되는지
     개별 재료 때문인지★ 구분하라. 개별 재료면 그 재료를 적어라.
   · 유가·금리·환율이 주간 1% 이상 움직였다면 ★수혜/피해 진영의 시총 비중 배율★을 계산하라.
     (2026-09-01 실측: 유가 피해 7.28% vs 수혜 1.77% = 4.12배. 정유주가 31위 밖이라 지수에
     직접 수혜주가 없다 → 유가 급등은 KOSPI에 구조적 순부정.) 이 비대칭은 매크로 변수마다 다르다.
 · 주간 업종 등락률 상위·하위 5개, 주간 등락률 상하위 10종목
 · 원/달러 주간 레인지와 방향, 국고채 3Y/10Y 주간 bp 변화
 · 해외: S&P500·나스닥·다우·러셀2000·★SOX★ 주간 등락률, 니케이·항셍·상해·대만 주간,
   미10Y/30Y 주간 bp, DXY·WTI·금 주간, ★EWY 주간 등락률★
 · 지수 나열이 아니라 "무엇이 주간 시장을 움직였는가"의 인과를 규명할 것

[2] ★★예측 채점 (이 리포트의 존재 이유)
 5거래일 각각에 대해 아래를 표로 만들어라.
 · 아침 브리핑 확률가중 기대 등락률 vs 실제 → 괴리(%p)
 · 저녁 마감 리포트 확률가중 기대 등락률 vs 실제 → 괴리(%p)
 · 종가 점추정 vs 실제 종가 → 오차(p), 80% 구간 포함 여부
 · 시나리오 3개 중 어느 밴드가 적중했는지, 확률 배분이 사후적으로 적절했는지
 · 삼성전자·삼성전자우·SK하이닉스 개별 예측 vs 실제 → 괴리(%p)와 배율
 · 나머지 블록 예측 vs 실제 역산 → 괴리(%p)와 배율
 주간 집계로 아래를 반드시 ★bash/python으로 실제 계산★하라 — 암산 금지:
 · 아침 vs 저녁 ★평균절대오차(MAE)★와 ★평균편향(bias)★ — 어느 쪽이 더 정확했는가
 · 방향 적중률(5회 중 몇 회), 시나리오 밴드 적중률, 80% 구간 포함률(이론값 80%와 비교)
 · ★클러스터 단위 채점(신설): 아침 브리핑이 제시한 ★클러스터별 갭 함의★와 실제 클러스터 등락률을
   5거래일 전부 대조하라. 클러스터별 MAE와 방향 적중률을 계산하고 ★어느 클러스터 판단이 구조적으로
   빗나가는지★ 지목하라. 반도체 클러스터만 맞히고 나머지 5개를 계속 틀리면 지수는 우연히 맞은 것이다.
 · ★5블록 채점: 삼성전자 / 삼성전자우 / SK하이닉스 / 상위31 내 기타 대형주(약 25.9%) /
   32위 이하 중소형주(약 22.8%). 기존 4블록의 '나머지'를 쪼갠 것이며, 두 하위 블록이 서로 반대로
   움직인 날이 며칠이었는지 세어라. 반대로 움직인 날이 많을수록 4블록 모델의 정보 손실이 크다.
 · ★오차 귀속 분해: 일별 총오차를 삼성전자/삼성전자우/SK하이닉스/나머지 블록 몫으로 나누고
   각각의 기여 %를 계산하라. 주간 합산으로 ★어느 입력이 오차의 주범이었는지★ 지목하라.
 · 계통 편향 진단: 과소예측 경향인가 과대예측 경향인가. 상방/기본/하방 확률 배분에 구조적
   쏠림이 있는가. 밴드 폭이 과대(맞히기 쉽게 넓힘)인가 과소인가.
 · 예측불가 개별이벤트(급작스런 공시·정책 발표 등)가 오차에 기여한 몫을 분리하고,
   ★그것이 예측을 우연히 도왔는지 해쳤는지★를 구분해 적어라. 우연한 적중은 실력이 아니다.

[3] ★★교훈 추출 (다음 주 세션이 실제로 쓸 수 있게)
 · 이번 주에 반복된 실수를 ★구체적으로★ 지목하라. 아래는 최근 지속 지적된 항목이므로
   해당 여부를 매주 명시적으로 판정하라:
   (a) 나머지 블록(약 49%)을 상수처럼 취급하는 오류 — 4회 연속 지적됨.
       ★2026-09-01에 정량 근거가 확정됐다: 그 49% 중 ★52.9%가 시총 31위 안의 대형주★이고
       47.1%만 32위 이하다. 같은 '나머지' 안에서 전력기기·기계(-0.137%p)와 자동차·조선(+0.028%p)이
       하루에 8%p 넘게 갈렸다. ★해법은 5블록 분해(상위31 대형주 25.9% + 중소형주 22.8%로 분리)이며
       이번 주 리포트가 실제로 그렇게 했는지 판정하라. 안 했으면 그것을 이번 주 최대 실수로 적어라.
   (b) 기본 시나리오에 과도한 확률(45%+)을 배정하고 반복 이탈하는 패턴
   (c) 가설을 세운 다음 날 그 가설의 반증 조건을 체크포인트에 넣지 않는 것
   (d) 순환매의 '다음 목적지'를 지목하지 않고 '기존 주도주 되돌림 여부'만 묻는 것
   (e) 확정 종가가 아닌 장중값·시간외를 종가로 오인하는 것
   (f) 야간 해외 변수를 근거로 전일 저녁 예측을 과도하게 수정해 오히려 오차를 키우는 것
 · 각 교훈에 ★다음 주에 검증 가능한 구체적 행동 지침★을 붙여라. 추상적 반성 금지.
 · 이번 주에 새로 검증된 조회 경로와 실패한 경로를 정리하라(인프라 메모).

[4] 다음 주 관전 포인트
 · 다음 주 이벤트 캘린더(KST): 국내외 지표·중앙은행·주요 실적(반도체/빅테크)·선물옵션 만기·
   배당락·지수 리밸런싱. 각각 컨센서스·직전치와 KOSPI 민감도(상/중/하).
   ※ 만기는 검색하지 말고 bash로 계산하라(KOSPI200 옵션 만기 = 매월 둘째 목요일,
     선물 만기 = 3·6·9·12월 둘째 목요일).
   ※ 현 연준 의장은 케빈 워시(Kevin Warsh, 2026-05-22 취임)다. 파월로 쓰지 마라.
   ※ 2026-08-27 금통위에서 기준금리를 3.00%로 연속 인상했다(긴축 사이클, 시장은 종착지를
     3.25% 이상으로 본다). 이 국면을 서술의 기본 배경으로 깔아라.
 · 미결 리스크 항목(예: 미 2차 반도체 관세 검토 진행 상황)과 각각의 트리거·임계값
 · ★다음 주 주간 밴드 전망: KOSPI 주간 고가·저가·금요일 종가 점추정과 80% 구간.
   ★단 월요일 개장 갭이나 일별 점추정은 하지 마라★ — 그것은 개장 전 브리핑의 몫이다.
 · σ는 반드시 계산해 제시하라(주간 실현변동성 RMS, 산식 노출). 쇼크성 이상치가 포함된 구간이면
   사전분포로 수축하고 그 가중치를 밝혀라.
 · 금지: "변동성 확대에 유의" 같은 회피성 마무리. 근거 있는 폭으로 좁힐 것.

■ ★★HTML 출력 규격 — 처음부터 "이메일용 HTML"로 작성하라 (가장 중요)
수신자는 samsung.com 계정, 즉 ★Outlook/Exchange★다. Outlook 데스크톱은 Word 렌더링 엔진을 쓰므로
아래를 ★전부 무시★한다: <style> 블록, CSS 클래스 선택자, display:grid, display:flex,
display:inline-block, border-radius, @media, @supports, <svg>, CSS 변수.
쓰면 스타일이 통째로 벗겨져 맨 텍스트로 보인다(2026-08-21에 실제 사고).
파일 버전과 메일 버전을 나누지 말고 ★하나의 이메일 호환 HTML★을 만들어 둘 다에 쓴다.
필수 규칙:
- ★레이아웃은 전부 <table>. 바깥은 <table width="100%"><tr><td align="center"> 안에 <table width="880">.
  다단은 <tr>에 <td width="25%">/<td width="33%">를 나열하고 그 안에 중첩 테이블을 넣는다.
- ★스타일은 전부 인라인(style="..."). <style> 블록과 class 선택자 금지.
  <!DOCTYPE>, <html>, <head>, <body> 태그도 넣지 말고 <div>부터 시작한다.
- ★용량 절약: font-family·font-size·기본 color는 최상단 <div>와 바깥 <td>에 한 번만 지정하고,
  수백 개의 데이터 셀에서는 style 속성을 통째로 생략하라 — Word 엔진은 상위에서 상속한다.
  표는 cellpadding="6" cellspacing="0"을 쓰고 셀마다 padding/border를 반복하지 마라.
  행 구분은 border-bottom 대신 지브라 배경(<tr bgcolor="#fafbf6">)으로만.
  헤더행은 <tr bgcolor="#e4e9dc"> + 셀 내용을 <b>로. 색상 강조는 <b><font color="#d03b3b">…</font></b>.
  HTML 엔티티 대신 UTF-8 리터럴 문자를 직접 써라. &amp; &lt; &gt; 만 엔티티로 남긴다.
  (이 기법으로 182KB → 65KB 실측)
- ★SVG 금지. 막대 차트는 bgcolor를 칠한 고정폭 <td>를 중첩 테이블로 배치해 그린다(라벨+범례 포함).
  ★주간 리포트에는 최소 2종을 이 방식으로 시각화하라:
    (1) 일별 투자자별 순매수 발산 막대(좌 순매도/우 순매수)
    (2) ★일별 예측오차 막대(아침 vs 저녁 나란히)★ — 이 리포트의 시각적 핵심이다
- 계산식 박스는 bgcolor="#16301f" 셀에 font-family:Consolas,monospace를 주고 줄바꿈은 <br>,
  들여쓰기는 공백 문자로 직접 넣는다(<pre>나 white-space:pre는 Outlook에서 불안정).
- 팔레트: 배경 #f2f4ec / 밴드 #16301f / 상승 #d03b3b / 하락 #2a78d6 / 선 #d8ddd0 / 카드 #ffffff /
  강조박스 #fdf6e8 — 차분한 페이퍼 톤.
구성 순서: ① 헤더(주간 범위·실제 발행 시각 명시) → ② 주간 총평 → ②-b ★데이터 신뢰도 박스★ →
③ KPI 타일 8개(주간 등락률·수급·채점 성적) → ④ ★예측 채점 표(5거래일 × 아침/저녁)★ →
⑤ ★오차 귀속 분해 + 일별 오차 막대★ → ⑥ 교훈 카드 5~7장 → ⑦ "상세" 구분선 →
⑧ 주간 시장 상세(w1 지수·w2 수급·w3 업종/특징주·w4 해외·w5 다음 주 캘린더) → ⑨ 면책·출처.
- ★②-b 데이터 신뢰도 박스는 필수다. 총평 바로 다음, KPI 타일 앞에 배치. 확보 항목 수 /
  확인 불가 항목 수를 명시하고 ★확인 불가 항목을 전부 나열하되 각각에 "채점 영향: 있음/낮음/없음"★을
  붙여라. 영향 "있음"은 굵게. 면책 각주로 내리지 마라. 0건이면 "전 항목 확보"라고 강조하라.
- 헤더에 ★실제 발행 시각★을 적어라. 예정 시각을 찍어놓고 그 뒤에 내보내는 것은 허위 표기다.
- 교훈 카드 1장 = 교훈 1건. 제목 15자 내외 명사형, 불릿 2개에 각각 <b>실제 수치</b> 포함,
  하단에 "다음 주 행동 지침" 1~2문장. 카드 상단 3px 강조선은 <tr><td height="3" bgcolor="…"></td></tr>로.
- 등락 수치는 항상 색+▲▼ 기호를 붙이고, ▲▼는 반드시 숫자 부호와 일치시킬 것.
- 메일에서 동작하지 않으므로 ★앵커 링크(href="#w1")를 아예 넣지 마라.
- ★Gmail 클리핑 한도 102,400 바이트를 넘기지 말 것. 발송 전 len(html.encode())를 반드시 출력해
  확인하고, 초과하면 위 용량 절약 기법을 먼저 적용하고 그래도 넘치면 표 행을 줄여 맞춘다(목표 95KB 이하).
- ★발송 전 자동 검증: '<style' / 'var(--' / 'class="' / 'display:grid' / 'inline-block' / '@media' /
  '<svg' / 'http' / '<script' / 'border-radius' / '<!DOCTYPE' / 'href="#' / '<html' / '<head' / '<body'
  문자열이 ★하나도 없어야★ 한다. 파이썬 assert로 확인하고, 태그 개폐 균형(table/tr/td/div/font/b)과
  ▲▼ 기호-부호 일치도 정규식으로 자동 검사하라. (2026-08-29 회차의 verify.py 패턴을 재현하라)
- playwright 렌더링 검증(executablePath는 find로 /opt/pw-browsers 아래 chromium 실제 경로 지정,
  playwright install 금지). 1240px에서 document.scrollWidth == clientWidth 여야 한다.
  시간이 모자라면 이 검증은 생략하고 문자열 assert만으로 발송하라.

■ 이메일 발송
1) SendUserFile로 HTML 파일을 사용자에게 전달한다(브라우저 확인용 최소 doctype 래퍼 preview 허용).
   ★files 파라미터는 반드시 배열로 넘겨라(문자열로 넘기면 JSON 파싱 오류가 난다).
2) Gmail MCP(mcp__Gmail__send_message, ToolSearch로 먼저 로드)로 ★giho.jeong@samsung.com★ 에 발송.
   제목: "[KOSPI 주간 리뷰] M월 D일~D일 — {한 줄 총평}".
3) ★htmlBody에 HTML 전문을 그대로 넣는다. 첨부파일 금지. ★plain-text body 파라미터를 넣지 마라★
   — multipart/alternative가 되면 클라이언트가 텍스트 파트를 골라 서식 없이 보일 수 있다. HTML 단독.
   ★서브에이전트에 위임하지 마라 — Agent 호출은 오토 모드 분류기에 차단된다(2026-09-01 실증).
   HTML이 커도 ★본체가 직접 htmlBody에 전문을 옮겨 적어라★. 약 60,000자 기준 5분 정도 걸리므로
   그 시간을 발행 시각 계산에 미리 반영하라. 요약·수정 절대 금지, body 파라미터 금지,
   DOCTYPE/html/head/body 추가 금지.
   전사 전에 `fold -w 3000 파일 > f.txt`로 조각내 전량을 읽고, 발송 후 get_message로 반영을 확인하라.
   2026-09-01에 74,756바이트(59,623자)를 이 방식으로 직접 전사해 발송 성공했다.
4) 메일은 한 번만 보낸다. 발송 후 오류를 발견하면 ★정정 메일을 자동으로 보내지 말고★
   PushNotification과 Drive 기준선 문서(REV 표기)에 정정 내용을 적어라.
5) Gmail 도구가 없거나 인증 실패면 재시도하지 말고 그 사실을 PushNotification에 명시하라.

■ 기준선 저장 (필수 · 메일 발송 뒤에)
mcp__Google_Drive__create_file로 parentId='1hLv3NpWB3I70y33ZSpmhYgU6MGU-0cbK',
title=`kospi_macro_baseline_YYYYMMDD_YYYYMMDD`(주 시작~종료), contentMimeType='text/plain'로 저장한다.
다음 주 세션들이 이 파일을 읽어 주간 인과 서술의 출발점으로 쓰므로 파싱 가능하게 적어라.
 - 주간 범위, 작성 시각(★Drive 서버 시각 기준★), bash date 편차(있었다면)
 - ★금요일 종가 기준선: KOSPI, KOSDAQ, 삼성전자, 삼성전자우, SK하이닉스, SK스퀘어,
   원달러, 국고채 3Y/10Y, 미10Y/30Y, SOX, EWY
 - 삼성전자·삼성전자우·SK하이닉스 상장주식수와 KOSPI 시총 비중(자체 계산, 산식 포함)
 - 주간 수급 누적(외국인/기관/개인/기타법인)과 일별 추이, 현·선물 방향 일치 여부
 - 주간 실현변동성 σ(산식 포함)
 - ★예측 채점 집계: 아침/저녁 MAE·bias·방향적중률·밴드적중률·80% 구간 포함률, 오차 귀속 분해
 - ★다음 주에 반드시 교정할 교훈 3~5개(구체적 행동 지침 형태)
 - 다음 주 이벤트 캘린더, 미결 리스크와 트리거·임계값
 - 다음 주 주간 밴드 전망(주간 고가·저가·금요일 종가 점추정과 80% 구간)
 - 확인 불가 항목 목록과 각각의 채점 영향
 - ★인프라 메모: 새로 검증된 조회 경로, 실패한 경로, 시계 이상 여부
 - 같은 제목 파일이 이미 있으면 새로 만들고 이전 파일을 trash_file로 정리해 중복을 남기지 마라.

■ 마무리
PushNotification을 보낸다. <routine_summary> 태그 안에 첫 문장은 주간 총평 한 줄, 이어서
주간 등락률·채점 성적(아침/저녁 MAE와 방향적중률)·오차 주범 입력·다음 주 최대 변수를 담아
세션을 열지 않고도 판단할 수 있게 쓸 것. 메일 발송 성공 여부, Drive 기준선 저장 여부,
★실제 발행 시각과 08:30 대비 초과 분수★, ★확인 불가 항목 수★를 각각 한 줄로 포함하라.

■ 원칙
- 수치는 반드시 실제 조회로 확보하라. 경로를 다 돌린 뒤에도 못 구한 항목만 "확인 불가"로 남기고
  그럴듯한 추정치로 채우지 말 것.
- 채점 계산은 bash/python으로 실제로 돌려 검산하라 — 암산 금지. 지수 분해는 모델 잔차 0을 확인하라.
- 직접 계산한 값(시총 비중·지수 기여도·MAE·오차 귀속)은 계산식과 전제를 함께 보여주고
  거래소 공식치가 아님을 면책에 명시하라.
- 매체별 수급 집계가 다르면 편차 범위를 밝히고 어느 값을 기준으로 삼았는지 적어라
  (3주체 합이 0으로 닫히는 출처를 채택).
- ★채점은 자기변호가 아니라 진단이다. 틀린 것을 축소하지 말고, 왜 틀렸는지 입력 단위로 분해하라.
  우연히 맞은 것은 맞았다고 쓰되 실력이 아니라고 명시하라.
- Word/docx 파일은 만들지 말 것. 첨부파일도 보내지 말 것. 전문을 메일 본문(htmlBody)에 넣는다.